TODO Later

Questions 

* ajouter une option dans nlc pour désassembler un .nlm ?
* interopérabilité de NL ?

DONE

* ~~gap découvert en creusant la capture de closures : une closure imbriquée à 2+ niveaux référençant une variable capturée par une closure englobante (pas la sienne propre) échouait à la compilation (`undefined variable`)~~ — `compile_closure` (`nl-codegen/src/expr.rs`) ne résolvait les candidats capturés que via `lookup_local` (les locaux/params de la portée englobante), jamais via `self.captured_fields` (les propres captures de l'émetteur englobant, quand celui-ci compile lui-même le corps d'une closure). Fix : fallback sur `self.captured_fields.get(name)` quand `lookup_local` échoue, avec un nouveau `CaptureSource` (`Local(u16)` vs `Recaptured`) pour distinguer, au site de création, un `Load` de slot local d'une relecture de `this.name` (nouveau `emit_get_captured_field_raw`, qui copie la valeur/le `Box<T>` brut sans le déballer). Vérifié côté VM que `GET_FIELD`/`SET_FIELD` ne résolvent un champ que par nom (la classe statique du field-ref du constant-pool est ignorée), ce qui permet de réutiliser `captured_field_ref` tel quel pour ce nouveau chemin. Nouveau test `tests/phase9_0150_closure_nested_recapture.yaml` (re-capture en lecture seule + re-capture mutée/boxée propagée sur 2 niveaux, visible de retour dans la portée d'origine) ; confirmé reproductible avant fix (`git stash` + run du test : `codegen error: unsupported construct: undefined variable 'x'`). 142 tests maison + 14/14 nlvm-specs toujours verts.

* ~~la mutation de variable capturée par une closure (counter++) — les closures capturent par valeur (snapshot), pas par référence comme l'indique la doc~~ — capture par référence implémentée (specs.md § Variable capture, vm.md § Variable capture and boxing) : une variable capturée est boxée (`Box<T>`, même mécanisme que les paramètres `ref`) si et seulement si elle est à la fois (a) référencée à l'intérieur d'au moins une closure et (b) mutée quelque part dans la méthode englobante (par la closure elle-même ou par le code englobant, `nl_codegen::closure::boxed_captures`/`boxed_captures_in_block`) — sinon elle reste copiée par valeur (vm.md l'autorise explicitement pour les captures en lecture seule). Limitation assumée et documentée en commentaire : seules les variables/paramètres **explicitement typés** peuvent être boxés — `nl_syntax::monomorphize::collect_closure_box_requests` (miroir AST-only de `collect_ref_box_requests`, qui alimente la même génération de classes `Box<T>`) ne peut pas déduire un type pour une capture `auto` sans un vrai vérificateur de types à ce stade ; une capture `auto` mutée garde donc le comportement par valeur existant. 141 tests maison (nouveau `tests/phase9_0140_closure_capture_boxing.yaml` : paramètre capturé, variable déclarée sans initialiseur puis boxée, deux closures distinctes partageant la même box) + 14/14 nlvm-specs toujours verts.

* ~~les déclarations explicites de type fonction ((int) => bool x = ...)~~ — `Type::Function { params, return_type, throws }` ajouté à `nl-syntax::ast` (specs.md § Function type assignment) ; parsing dans `parse_type_atom` (`(Type, ...) => ReturnType [throws ...]`, sans ambiguïté en position de type) + désambiguïsation en position de statement (`looks_like_function_type_start`, tentative-parse-et-retour-arrière comme les closures, distingue `(int) => int f = ...;` d'une expression parenthésée/closure niladique). `typedef` reste hors scope (mot-clé réservé mais jamais implémenté, gap pré-existant distinct). Décision d'architecture clé côté `nl-codegen` : pas de nouvelle variante `ExprTy` — `expr_ty_of(Type::Function)` réutilise `ExprTy::Closure{params, return_ty, fqcn}` avec un `fqcn` *placeholder* (`FUNCTION_TYPE_FQCN`), valide parce que `INVOKE_CLOSURE` (nl-vm) dispatche uniquement sur la classe *runtime* du receiver + nom/descriptor — la classe statique du constant-pool method_ref est décodée puis jetée. Ça réutilise tel quel tout `compile_call`/`compile_closure_invoke`/`compile_array_method_call` sans y toucher ; seul `coerce_value` gagne une branche dédiée (`closure_shape_eq`, structurelle, `fqcn` ignoré) pour accepter une assignation/un argument dont les formes correspondent. Limitation assumée et documentée (`ExprTy::Closure`'s doc comment) : pas de vrai target-typing (specs.md règle de déduction #5) — un literal de closure assigné à un type fonction plus large (ex. `(int) => float k = (int n) => n;`) garde son propre descriptor déduit et se fait rejeter par `coerce_value` plutôt que d'insérer le widening, faute de mécanisme d'adaptateur (et parce que `compile_closure` ne coerce déjà pas ses `return` vers un type attendu, gap préexistant plus large). Deux bugs préexistants découverts et corrigés en cours de route (indépendants des types fonction explicites, juste jamais exercés avant) : (1) `count_params`/descriptor-parsing côté `nl-vm::interpreter` faisait un `split(", ")` naïf, cassé dès qu'un paramètre est lui-même un type composé contenant sa propre virgule top-level (type fonction imbriqué, ou générique mangled `system.Map<K, V>`) — fix : split depth-aware sur `(`/`)` et `<`/`>` ; (2) `compile_closure` construisait le descriptor de sa propre méthode `invoke` directement depuis les `Type` déclarés des paramètres (préservant les unions, ex. `string|null`) au lieu de repasser par `ExprTy` (qui les collapse toujours vers le membre non-null) comme le fait tout site d'appel — un closure `auto`-typé avec un paramètre union, appelé via son identifiant nu, plantait donc déjà à l'exécution (`invoke` introuvable) avant ce chantier. 5 tests maison `tests/phase9_00{90,100,110,120,130}_*.yaml` (var decl explicite, param+retour de type fonction, champ de type fonction, union+throws, régression closure `auto`+union). 140 tests maison + 14/14 nlvm-specs toujours verts.
* ~~nlc -l (linting)~~
* ~~enum~~ — basique + typé (int/string), méthodes/propriétés statiques et d'instance, `from`/`tryFrom`, exhaustivité de `match` (E047). Limitation assumée : pas de vraie storage statique VM (pas de `GET_STATIC`/`SET_STATIC`/`<clinit>`) — les constantes de cas sont recompilées (constantes de compilation) à chaque référence plutôt que lues depuis un stockage statique runtime ; les "static properties" custom au-delà des cas ne sont donc pas supportées (aucun exemple des specs n'en a besoin).
* ~~l'opérateur ?? (coalescing)~~ — `??` et `?:` (elvis) implémentés ensemble (même niveau de précédence, specs.md). Limitation assumée, cohérente avec `Ternary` déjà existant : nl-sema reste permissif sur le type (pas de nouveau code E vérifiant que l'opérande gauche est bien nullable), et le type résultat est approximé (opérande gauche sans `null`) plutôt que le vrai "common result type" à 5 règles — `nl-codegen` fait juste un `coerce_value` de l'opérande droit vers celui de gauche, comme pour le ternaire.
* ~~tests 11/14 nlvm-specs (smart-cast narrowing)~~ — narrowing flow-sensible implémentée (compiler.md § Type narrowing) côté `nl-sema` (overlay `narrowed: HashMap<id, Type>` par-dessus la résolution de variable, calculé depuis les conditions `!= null`/`== null`/`instanceof`/`&&`/`||`, appliqué aux branches `if`/ternaire + narrowing "early exit" qui survit après le `if`, invalidé à la réassignation) et côté `nl-codegen` (contrepartie plus réduite : seul `instanceof` a besoin d'un équivalent côté codegen, car les unions nullables s'effacent déjà vers leur membre non-null dans `ExprTy` indépendamment du narrowing — `with_instanceof_narrowing` override temporairement l'`ExprTy` d'un local dans la branche `then` de `compile_if`). Suite nlvm-specs passée de 11/14 à 13/14 (130 tests maison toujours verts).
* ~~m5_0020_instanceof.yaml (13/14 → 14/14)~~ — le dernier échec n'était pas un bug de la fixture : commit `03a3fa9c` de nlvm-specs (2026-07-18, v0.8.44) a clarifié compiler.md § Union type compatibility (« subtype thereof » couvre explicitement l'implémentation d'interface, pas seulement `extends`) et corrigé la fixture (`Animal animal = dog` → `Animal|null animal = dog`). Le vrai bug était côté `nl-sema` : `checker::is_object_assignable` (seule fonction qui connaît `implements`/subclassing, `types::is_assignable` restant purement structurel) ne matchait que `Type::Named` des deux côtés et retournait `false` dès que la cible était une union — donc `Dog implements Animal` n'était jamais reconnu comme assignable à `Animal|null`. Fix : `is_object_assignable` itère maintenant sur `types::members(...)` des deux côtés (comme `types::is_assignable`) au lieu d'exiger un `Type::Named` nu. Suite nlvm-specs **14/14**, 130 tests maison toujours verts.
* ~~stack trace info~~ — chantier en 4 étapes (voir journal/journal_02_stack_trace.md) : (1) `line_table` peuplée en codegen (granularité statement, dédupliquée par ligne) ; (2) pile de frames NL walkable en parallèle de la récursion Rust native de l'interpréteur (`nl_vm::call_stack`, `thread_local!`, RAII pour le pop) ; (3) garde de profondeur d'appel → `StackOverflowException` catchable (seuil `MAX_CALL_DEPTH=150` calibré empiriquement, avec pile 8 MiB alignée entre thread principal et `system.thread.Thread` spawnés) ; (4) `Exception.stackTrace: ExecutionPoint[]` capturé nativement (bypass bytecode) juste avant le retour du constructeur racine `Exception`, en excluant les frames du chaînage de constructeurs de l'exception elle-même (calculé via la profondeur `extends` de la classe runtime jusqu'à `Exception`), plus stderr enrichi d'une ligne `at file:line` par frame sur exception non attrapée. Limitation assumée : `ExecutionPoint` n'a que `{file, line}` (conforme aux specs), pas de nom de méthode. 132 tests maison + 14/14 nlvm-specs.