TODO Later

* ~~nlc -l (linting)~~
* stack trace info
* ~~enum~~ — basique + typé (int/string), méthodes/propriétés statiques et d'instance, `from`/`tryFrom`, exhaustivité de `match` (E047). Limitation assumée : pas de vraie storage statique VM (pas de `GET_STATIC`/`SET_STATIC`/`<clinit>`) — les constantes de cas sont recompilées (constantes de compilation) à chaque référence plutôt que lues depuis un stockage statique runtime ; les "static properties" custom au-delà des cas ne sont donc pas supportées (aucun exemple des specs n'en a besoin).
* ~~l'opérateur ?? (coalescing)~~ — `??` et `?:` (elvis) implémentés ensemble (même niveau de précédence, specs.md). Limitation assumée, cohérente avec `Ternary` déjà existant : nl-sema reste permissif sur le type (pas de nouveau code E vérifiant que l'opérande gauche est bien nullable), et le type résultat est approximé (opérande gauche sans `null`) plutôt que le vrai "common result type" à 5 règles — `nl-codegen` fait juste un `coerce_value` de l'opérande droit vers celui de gauche, comme pour le ternaire.
* les déclarations explicites de type fonction ((int) => bool x = ...)
* la mutation de variable capturée par une closure (counter++) — les closures capturent par valeur (snapshot), pas par référence comme l'indique la doc
* ~~tests 11/14 nlvm-specs (smart-cast narrowing)~~ — narrowing flow-sensible implémentée (compiler.md § Type narrowing) côté `nl-sema` (overlay `narrowed: HashMap<id, Type>` par-dessus la résolution de variable, calculé depuis les conditions `!= null`/`== null`/`instanceof`/`&&`/`||`, appliqué aux branches `if`/ternaire + narrowing "early exit" qui survit après le `if`, invalidé à la réassignation) et côté `nl-codegen` (contrepartie plus réduite : seul `instanceof` a besoin d'un équivalent côté codegen, car les unions nullables s'effacent déjà vers leur membre non-null dans `ExprTy` indépendamment du narrowing — `with_instanceof_narrowing` override temporairement l'`ExprTy` d'un local dans la branche `then` de `compile_if`). Suite nlvm-specs passée de 11/14 à 13/14 (130 tests maison toujours verts).
* ~~m5_0020_instanceof.yaml (13/14 → 14/14)~~ — le dernier échec n'était pas un bug de la fixture : commit `03a3fa9c` de nlvm-specs (2026-07-18, v0.8.44) a clarifié compiler.md § Union type compatibility (« subtype thereof » couvre explicitement l'implémentation d'interface, pas seulement `extends`) et corrigé la fixture (`Animal animal = dog` → `Animal|null animal = dog`). Le vrai bug était côté `nl-sema` : `checker::is_object_assignable` (seule fonction qui connaît `implements`/subclassing, `types::is_assignable` restant purement structurel) ne matchait que `Type::Named` des deux côtés et retournait `false` dès que la cible était une union — donc `Dog implements Animal` n'était jamais reconnu comme assignable à `Animal|null`. Fix : `is_object_assignable` itère maintenant sur `types::members(...)` des deux côtés (comme `types::is_assignable`) au lieu d'exiger un `Type::Named` nu. Suite nlvm-specs **14/14**, 130 tests maison toujours verts.